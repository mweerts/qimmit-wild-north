// @ts-check
import { defineConfig } from 'astro/config';

// Preprod canonical base until the public domain is configured.
// https://astro.build/config
export default defineConfig({
	site: 'https://qimmitwildnorth.projects.mweerts.dev',
});
